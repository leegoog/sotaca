<% title "your order" %>


<div class="order_detail">
	<p>
		Order#: <%= @order.order_nr %>
	</p>
	<p>
		Name: <%= "#{@order.first_name} #{@order.last_name}" %>
	</p>
		
	<p>
		Date#: <%= @order.created_at.to_formatted_s %>
	</p>
	<p>
		Payment Method: TODO
	</p>
	<p>
		Total: <%= number_to_currency (@order.total_price.to_money.exchange_to(session[:currency])) %>
	</p>
		
	<p>
		Shipping Adress: <%= "#{@order.street} #{@order.house_nr}, #{@order.zipcode} #{@order.city}, #{@order.country}" %>
	</p>

</div>
